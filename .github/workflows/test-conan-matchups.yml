name: Test Conan matchups

on:
  push:
    branches: [test/conan-matchups]

permissions:
  contents: write

jobs:
  simulate:
    if: github.event.head_commit.message == 'Run Conan matchup simulation v5'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: test/conan-matchups
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run matchup simulation
        run: |
          set +e
          node tools/t_conan_matchups.js > tools/conan_matchup_run.log 2>&1
          code=$?
          echo $code > tools/conan_matchup_exit_code.txt
          cat tools/conan_matchup_run.log
          exit 0
      - name: Commit results and log
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f tools/conan_matchup_run.log tools/conan_matchup_exit_code.txt
          [ ! -f tools/conan_matchup_rows.json ] || git add -f tools/conan_matchup_rows.json
          [ ! -f tools/conan_matchup_results.json ] || git add -f tools/conan_matchup_results.json
          git commit -m "Record Conan matchup simulation" || true
          git push origin HEAD:test/conan-matchups
